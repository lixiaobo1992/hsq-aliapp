import {
  me,
  xmini,
  xPage,
} from '../../config/xmini';
import {
  mixins,
} from '/utils';

xPage({
  ...mixins,
  data: {},
  onLoad(query) {
    this.onPageInit(query);
  },

  onShow() {

  },

  onUnload() {

  },

});
